---
trigger: always_on
---

This app is a leetcode problem visual simulations based web UI app
built with Vite (JSX only), react, shadcn

Whenever you are implementing a new question just make a css and jsx files in @components/problems directory within current directory to implement new question when user is asking for it, follow other existing components style.